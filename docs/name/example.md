```scryfall-extended-multi
# all cards with Island in the name
name:island

# if you're feeling lazy, omit `name:` its all good!
island

# finds cards with the exact name "Island"
!island

# use quotes to include spaces in your search
name:"knight of"

# name supports regex searches, too
name:/^syr \w+,/
```